import { CountToColorPipe } from './count-to-color.pipe';

describe('CountToColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CountToColorPipe();
    expect(pipe).toBeTruthy();
  });
});
